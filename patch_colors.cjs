const fs = require('fs');
let code = fs.readFileSync('server.ts', 'utf-8');

const targetStr = `التنسيق والألوان (مهم جداً):\\n- استخدم تنسيق Markdown.\\n- يُمنع منعاً باتاً استخدام HTML لوضع خلفيات ملونة أو نصوص ملونة ثابتة للملاحظات (لأنها تصبح غير مقروءة في الوضع المظلم). اعتمد على تنسيق Markdown الافتراضي (مثل > للاقتباسات) بدلاً من ذلك.\\n- فقط إذا سأل التلميذ تحديداً عن رسم الدوال، أخبره عن قسم "دراسة ورسم دوال".`;

const replaceStr = `التنسيق والألوان (مهم جداً):\\n- استخدم تنسيق Markdown و HTML لتلوين العناوين والملاحظات المهمة لإبرازها.\\n- تجنب وضع خلفيات ملونة (background-color) لكي لا تتعارض مع الوضع المظلم، واكتفِ بتلوين النص (color) بألوان متناسقة ومقروءة في كلا الوضعين (مثل البرتقالي الفاتح أو الأزرق).\\n- فقط إذا سأل التلميذ تحديداً عن رسم الدوال، أخبره عن قسم "دراسة ورسم دوال".`;

code = code.replace(targetStr, replaceStr);

fs.writeFileSync('server.ts', code);
console.log('patched colors in server.ts');
